<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="NotatnikApp.Views.TasksPage">
    
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Add" Command="{Binding Add}" />
    </ContentPage.ToolbarItems>

    <ContentPage.Content>
        <StackLayout BackgroundColor="#0D0D0D">
            <CollectionView x:Name="Tasks" ItemsSource="{Binding Tasks}" >
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <StackLayout Padding="1">

                            <CheckBox IsChecked="{Binding done}" CheckedChanged="OnCheckBoxCheckedChanged"/>

                            <Label Text="{Binding name}" 
                                    FontSize="13" 
                                    TextColor="White" 
                                    BackgroundColor="#0D0D0D"
                                   FlowDirection="LeftToRight">
                            </Label>
                            <Button Text="Delete" Clicked="Button_Clicked"/>
                        </StackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>