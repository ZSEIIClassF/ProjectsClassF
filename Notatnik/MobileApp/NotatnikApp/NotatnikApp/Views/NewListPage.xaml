<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="NotatnikApp.Views.NewListPage"
             BackgroundColor="{DynamicResource blue}">
    
    <ContentPage.Content>
        <StackLayout Margin="15">
            <Entry Placeholder="np. zadania domowe" Text="{Binding Name, Mode=TwoWay}" BackgroundColor="{DynamicResource mediumBlue}"/>
            <Button Text="Add" Command="{Binding addList}" Style="{StaticResource button}"/>
        </StackLayout>
    </ContentPage.Content>
    
</ContentPage>